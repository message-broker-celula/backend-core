# Detecta IPs generando muchos errores 4xx/5xx contra el backend
# (fuzzing, scanners, intentos de exploit, floods de peticiones invalidas)
[Definition]
failregex = ^<HOST> - (4\d\d|5\d\d) ".*"
ignoreregex = ^<HOST> - 404 "GET /favicon.ico.*"
