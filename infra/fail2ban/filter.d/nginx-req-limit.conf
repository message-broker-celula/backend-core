# Detecta IPs que están siendo bloqueadas por limit_req/limit_conn de Nginx
# (status 429) usando el log_format "ratelimit" definido en backend.conf.
[Definition]
failregex = ^<HOST> - (429|444) ".*"
ignoreregex =
