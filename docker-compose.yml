services: 
  backend:
    container_name: backend-core-backend

    build: 
      context: .
      dockerfile: Dockerfile

    env_file:
      - .env

    environment:
      - .env
    
    ports:
      - "8000:8000"

    restart: unless-stopped

    tty: true 

